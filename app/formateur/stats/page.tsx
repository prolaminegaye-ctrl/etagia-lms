'use client'
export default function StatsPage() {
  return (
    <div>
      <h1 style={{fontSize:'26px',fontWeight:'700',marginBottom:'1rem',fontFamily:'var(--font-display)'}}>Statistiques</h1>
      <div style={{background:'var(--bg-card)',border:'1px solid var(--border)',borderRadius:'16px',padding:'3rem',textAlign:'center'}}>
        <div style={{fontSize:'48px',marginBottom:'1rem'}}>🚧</div>
        <p style={{color:'var(--text-secondary)'}}>Module en cours de développement — disponible prochainement</p>
      </div>
    </div>
  )
}
